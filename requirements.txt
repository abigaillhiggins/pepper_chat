# Core LangChain and OpenAI dependencies
langchain>=0.1.0
langchain-openai>=0.0.1
langchain-community>=0.0.1
openai>=1.0.0

# Environment and configuration
python-dotenv>=0.19.0

# Speech processing
vosk>=0.3.45
sounddevice>=0.4.5
numpy>=1.20.0

# HTTP requests and networking
requests>=2.31.0
urllib3>=2.0.0

# Date and time handling (for Australian timezone)
pytz>=2023.3

# Regular expressions (built-in but ensuring compatibility)
regex>=2023.0.0

# Threading and concurrency (built-in but ensuring compatibility)

# Legacy TTS system (ElevenLabs)
elevenlabs>=0.2.0

# Optional: For enhanced logging and debugging
# logging (built-in)
# datetime (built-in)
# time (built-in)
# re (built-in)
# os (built-in)
# threading (built-in) 